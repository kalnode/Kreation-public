import { rawConfigValues } from "./app.config.rawValues"

// TODO: Expose these values to Nuxt Studio interface, such that they can be editable there.
// https://github.com/kalnode/Kreation/issues/7

export default defineAppConfig(rawConfigValues)
